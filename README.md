# python-course-assignments
This program converts an area measured in pixels to mm² and normalizes it according to another given value.
It includes three input methods: interactive input, command-line arguments, and a GUI.

I used GitHub Copilot (GPT-4) to generate the code.
Prompt: “Generate a Python program that converts pixel area to mm² and normalizes it by another value, with interactive input, command-line arguments, and a Tkinter GUI.”
